# Static Traefik config — HTTP only (default until: adn-docker ssl enable)
api:
  dashboard: false

entryPoints:
  web:
    address: ":80"

providers:
  file:
    filename: /etc/traefik/dynamic.yml
    watch: true
